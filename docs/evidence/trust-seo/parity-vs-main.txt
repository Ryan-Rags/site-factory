check-delivered-parity.mjs — feat/trust-seo vs main@e9654c5
Both sides built with pnpm build:all on the same machine.

===== american-machine-specialty =====
5 pages compared: 0 byte-identical, 5 changed only in panel machinery, 0 regressed.
Named head exemptions taken (trust-seo). Each is a deliberate metadata
  change, scoped to the delta that was measured and to nothing else:
    + cardMetadataAdded  5 page(s): 404.html, about/index.html, contact/index.html, index.html, services/index.html
    + brandCardSwapped  5 page(s): 404.html, about/index.html, contact/index.html, index.html, services/index.html
✓ No client site changed what it renders.

===== industrial-machine-corp =====
5 pages compared: 0 byte-identical, 5 changed only in panel machinery, 0 regressed.
Named head exemptions taken (trust-seo). Each is a deliberate metadata
  change, scoped to the delta that was measured and to nothing else:
    + cardMetadataAdded  5 page(s): 404.html, about/index.html, contact/index.html, index.html, services/index.html
    + brandCardSwapped  5 page(s): 404.html, about/index.html, contact/index.html, index.html, services/index.html
✓ No client site changed what it renders.

===== kh-machine-works =====
5 pages compared: 0 byte-identical, 5 changed only in panel machinery, 0 regressed.
Named head exemptions taken (trust-seo). Each is a deliberate metadata
  change, scoped to the delta that was measured and to nothing else:
    + cardMetadataAdded  5 page(s): 404.html, about/index.html, contact/index.html, index.html, services/index.html
    + brandCardSwapped  5 page(s): 404.html, about/index.html, contact/index.html, index.html, services/index.html
✓ No client site changed what it renders.

===== ks-welding =====
5 pages compared: 0 byte-identical, 5 changed only in panel machinery, 0 regressed.
Named head exemptions taken (trust-seo). Each is a deliberate metadata
  change, scoped to the delta that was measured and to nothing else:
    + cardMetadataAdded  5 page(s): 404.html, about/index.html, contact/index.html, index.html, services/index.html
✓ No client site changed what it renders.

===== ks-welding-forge =====
5 pages compared: 0 byte-identical, 5 changed only in panel machinery, 0 regressed.
Named head exemptions taken (trust-seo). Each is a deliberate metadata
  change, scoped to the delta that was measured and to nothing else:
    + cardMetadataAdded  5 page(s): 404.html, about/index.html, contact/index.html, index.html, services/index.html
✓ No client site changed what it renders.

===== ks-welding-heritage =====
5 pages compared: 0 byte-identical, 5 changed only in panel machinery, 0 regressed.
Named head exemptions taken (trust-seo). Each is a deliberate metadata
  change, scoped to the delta that was measured and to nothing else:
    + cardMetadataAdded  5 page(s): 404.html, about/index.html, contact/index.html, index.html, services/index.html
✓ No client site changed what it renders.

===== ks-welding-precision =====
5 pages compared: 0 byte-identical, 5 changed only in panel machinery, 0 regressed.
Named head exemptions taken (trust-seo). Each is a deliberate metadata
  change, scoped to the delta that was measured and to nothing else:
    + cardMetadataAdded  5 page(s): 404.html, about/index.html, contact/index.html, index.html, services/index.html
✓ No client site changed what it renders.

===== kts-machine-shop =====
5 pages compared: 0 byte-identical, 5 changed only in panel machinery, 0 regressed.
Named head exemptions taken (trust-seo). Each is a deliberate metadata
  change, scoped to the delta that was measured and to nothing else:
    + cardMetadataAdded  5 page(s): 404.html, about/index.html, contact/index.html, index.html, services/index.html
    + brandCardSwapped  5 page(s): 404.html, about/index.html, contact/index.html, index.html, services/index.html
✓ No client site changed what it renders.

===== zz-fixture-phone-optional =====
  ✗ 404.html: missing from the candidate build
  ✗ about/index.html: missing from the candidate build
  ✗ contact/index.html: missing from the candidate build
  ✗ index.html: missing from the candidate build
  ✗ services/index.html: missing from the candidate build
5 pages compared: 0 byte-identical, 0 changed only in panel machinery, 5 regressed.
✗ 5 page(s) changed what they render. The acceptance is that an
  existing client's site is unaffected by panel work — see the note above.


--------------------------------------------------------------------
NOTE on zz-fixture-phone-optional above.

Not a regression. That directory exists in the baseline dist only because it
was built by hand during the Turnstile investigation; `build:all` deliberately
skips the `zz-fixture-` prefix, so the candidate side never produced it. The
gate is correctly reporting "missing from the candidate build" for a comparison
that should not have been attempted.

The eight real clients are the acceptance, and all eight report
"No client site changed what it renders" with only the two named exemptions:

  cardMetadataAdded   8/8 clients  (the four tags Seo.astro gained)
  brandCardSwapped    4/8 clients  (american-machine-specialty,
                                    industrial-machine-corp,
                                    kh-machine-works,
                                    kts-machine-shop)

The four K&S builds and ks-welding correctly do NOT take brandCardSwapped —
they already pointed at their generated cards.
