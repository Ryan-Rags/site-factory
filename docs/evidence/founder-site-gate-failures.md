# founder-site — demonstrated gate failures

Each new gate, shown failing against a deliberately broken `dist/` before being
relied on. Generated by a throwaway script; `dist/` was restored from a snapshot
after every case. 15 cases, 15 caught.

### check-indexable — robots.txt is switched to Disallow: / (the client-mockup default)

exit 1 (FAILS as intended)

```
checked 5 page(s): /, /about, /ai, /amenity, /sites
FAIL  check-indexable: 2 problem(s)
  - robots.txt blocks crawling: Disallow / — this site is indexed.
  - robots.txt has no explicit `Allow: /` — state the intent rather than relying on it.
```

### check-indexable — a page loses its canonical link

exit 1 (FAILS as intended)

```
checked 5 page(s): /, /about, /ai, /amenity, /sites
FAIL  check-indexable: 1 problem(s)
  - /ai: no <link rel="canonical">.
```

### check-metadata — /ai loses its meta description and /about reuses /ai's title

exit 1 (FAILS as intended)

```
checked 5 page(s), 4 distinct title(s)
FAIL  check-metadata: 2 problem(s)
  - /ai: <title> duplicates /about.
  - /ai: no meta description.
```

### check-metadata — an og:image points at a file that was never emitted

exit 1 (FAILS as intended)

```
checked 5 page(s), 5 distinct title(s)
FAIL  check-metadata: 1 problem(s)
  - /: og:image points at og/does-not-exist.png, which is not in dist/.
```

### check-amenity-wording — the forbidden category word appears in /amenity visible copy

exit 1 (FAILS as intended)

```
forbidden word "vending" clear across 5 page(s)
FAIL  check-amenity-wording: 2 problem(s)
  - /amenity: visible copy contains "vending" — …nt Alcove Markets Request a proposal Alcove Markets Premium vending for Curated smart markets for luxury residential bui… (the address belongs in a mailto: href, never as text).
  - /amenity: "vending" appears outside a mailto: href — …ass="shell stack"> <p class="eyebrow">Alcove Markets</p> <h1 class="h1">Premium vending for Curated smart markets for luxury residential buildings</h1> <p class…
```

### check-amenity-wording — the amenity address is rendered as link TEXT rather than only as an href

exit 1 (FAILS as intended)

```
forbidden word "vending" clear across 5 page(s)
FAIL  check-amenity-wording: 2 problem(s)
  - /amenity: visible copy contains "vending" — …Skip to main content Alcove Markets vending@raghubans.com Alcove Markets Curated smart markets f… (the address belongs in a mailto: href, never as text).
  - /amenity: "vending" appears outside a mailto: href — … </a> <nav class="topbar__nav" aria-label="Contact">  <a href="mailto:REDACTED">vending@raghubans.com</a> </nav>  </div> </header> <main id="main">  <section cl…
```

### check-amenity-wording — the forbidden word reaches a page title

exit 1 (FAILS as intended)

```
forbidden word "vending" clear across 5 page(s)
FAIL  check-amenity-wording: 2 problem(s)
  - /amenity: "vending" appears outside a mailto: href — …href="/sitemap-index.xml"><meta name="generator" content="Astro v5.18.2"><title>Vending for luxury buildings</title><meta name="description" content="Alcove Mar…
  - /amenity: <title> contains "vending".
```

### check-placeholders — one of the five DEMO_LINK cards is dropped

exit 1 (FAILS as intended)

```
12 placeholder slot(s) across 5 page(s), all as declared
FAIL  check-placeholders: 1 problem(s)
  - /sites: 4 DEMO_LINK slot(s), expected 5.
```

### check-placeholders — /sites links a live prospect demo on pages.dev

exit 1 (FAILS as intended)

```
13 placeholder slot(s) across 5 page(s), all as declared
FAIL  check-placeholders: 1 problem(s)
  - /sites: links https://ks-welding-preview.pages.dev — prospect/client demos are private noindex builds and must not be linked from an indexed page.
```

### check-no-forms — a contact form and an inline script are added

exit 1 (FAILS as intended)

```
5 page(s): no forms, no scripts, no third-party requests
FAIL  check-no-forms: 4 problem(s)
  - /about: contains a <form> — this site is mailto-only.
  - /about: contains <input> — no interactive controls on a static brochure.
  - /about: contains <button> — no interactive controls on a static brochure.
  - /about: contains a <script>. If this is intended, _headers must gain a real script-src CSP with this script's hash before the gate is relaxed.
```

### check-no-forms — a third-party font CDN is added to the head

exit 1 (FAILS as intended)

```
5 page(s): no forms, no scripts, no third-party requests
FAIL  check-no-forms: 1 problem(s)
  - /: fetches https://fonts.googleapis.com/css2?family=Inter from third-party host fonts.googleapis.com.
```

### check-headers — Referrer-Policy is removed from _headers

exit 1 (FAILS as intended)

```
FAIL  check-headers: 1 problem(s)
  - _headers is missing or has altered: Referrer-Policy: strict-origin-when-cross-origin
```

### check-textfit — a 600px-wide element is introduced, so 320px scrolls sideways

exit 1 (FAILS as intended)

```
serving own dist/ at http://127.0.0.1:61762
      5 route(s) fit 320px with no horizontal scroll
FAIL  check-textfit: 1 problem(s)
  - /sites: scrolls horizontally at 320px (scrollWidth 600). Offenders: div.(no class) right=600
```

### check-textfit — the hero name is truncated with an ellipsis

exit 1 (FAILS as intended)

```
serving own dist/ at http://127.0.0.1:56829
      5 route(s) fit 320px with no horizontal scroll
FAIL  check-textfit: 2 problem(s)
  - /: "Ryan Raghubans" is truncated at 320px (scrollWidth > clientWidth).
  - /: .hero__name has text-overflow:ellipsis — the name must never be abbreviated.
```

### check-textfit — the hidden separators are "tidied up" out of the h1

exit 1 (FAILS as intended)

```
serving own dist/ at http://127.0.0.1:52944
      5 route(s) fit 320px with no horizontal scroll
FAIL  check-textfit: 1 problem(s)
  - /: h1 text is "Ryan Raghubans Full-Stack Developer & Founder Bergen County, NJ", expected "Ryan Raghubans — Full-Stack Developer & Founder · Bergen County, NJ".
```
