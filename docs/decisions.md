# Decisions — the rulings ledger

Append-only. One dated line per ruling, each naming its source. A ruling that is
later narrowed or replaced gets a **new** line saying so; nothing above is edited
or deleted, because the point of the file is that a settled question stays
settled and a reversal is visible as a reversal.

**How to add one.** A stream that receives a durable ruling appends it in the PR
that completes the stream — the same PR that deletes the stream's
`PLAN-<stream>.md`. Date it, state it as a rule rather than as a narrative, and
cite the PR number or the plan file it came from.

**What belongs here.** Anything a later session would otherwise re-litigate or
guess at: a rule about money, safety, provenance, naming, gates or scope. Not
progress notes, not defect write-ups (those are `docs/known-issues.md`), not
anything the code already states plainly.

---

## Where the sources went

This ledger was seeded on 2026-08-12 by walking every root `PLAN-*.md`,
`PR-trust-seo.md` and every merged PR body. Those plan files were deleted in the
same pass. They remain readable, permanently, at the pre-cleanup commit
[`1b6225a`](https://github.com/Ryan-Rags/site-factory/tree/1b6225ac5fe0c69ab144aa7b742bbd5ebc64d393)
— so a code comment that says "see `PLAN-pipeline.md`'s backlog" resolves in one
hop from here:

| Deleted file | Permanent location |
|---|---|
| `PLAN.md` | *kept — repo scaffold notes, not a stream plan* |
| `PLAN-copy.md` | [blob@1b6225a](https://github.com/Ryan-Rags/site-factory/blob/1b6225ac5fe0c69ab144aa7b742bbd5ebc64d393/PLAN-copy.md) |
| `PLAN-demo.md` | [blob@1b6225a](https://github.com/Ryan-Rags/site-factory/blob/1b6225ac5fe0c69ab144aa7b742bbd5ebc64d393/PLAN-demo.md) |
| `PLAN-demo-support.md` | [blob@1b6225a](https://github.com/Ryan-Rags/site-factory/blob/1b6225ac5fe0c69ab144aa7b742bbd5ebc64d393/PLAN-demo-support.md) |
| `PLAN-design-families.md` | [blob@1b6225a](https://github.com/Ryan-Rags/site-factory/blob/1b6225ac5fe0c69ab144aa7b742bbd5ebc64d393/PLAN-design-families.md) |
| `PLAN-design-polish.md` | [blob@1b6225a](https://github.com/Ryan-Rags/site-factory/blob/1b6225ac5fe0c69ab144aa7b742bbd5ebc64d393/PLAN-design-polish.md) |
| `PLAN-discovery.md` | [blob@1b6225a](https://github.com/Ryan-Rags/site-factory/blob/1b6225ac5fe0c69ab144aa7b742bbd5ebc64d393/PLAN-discovery.md) |
| `PLAN-lead-flow-2.md` | [blob@1b6225a](https://github.com/Ryan-Rags/site-factory/blob/1b6225ac5fe0c69ab144aa7b742bbd5ebc64d393/PLAN-lead-flow-2.md) |
| `PLAN-mockup.md` | [blob@1b6225a](https://github.com/Ryan-Rags/site-factory/blob/1b6225ac5fe0c69ab144aa7b742bbd5ebc64d393/PLAN-mockup.md) |
| `PLAN-multi-client.md` | [blob@1b6225a](https://github.com/Ryan-Rags/site-factory/blob/1b6225ac5fe0c69ab144aa7b742bbd5ebc64d393/PLAN-multi-client.md) |
| `PLAN-pipeline.md` | [blob@1b6225a](https://github.com/Ryan-Rags/site-factory/blob/1b6225ac5fe0c69ab144aa7b742bbd5ebc64d393/PLAN-pipeline.md) |
| `PLAN-prospect-parity.md` | [blob@1b6225a](https://github.com/Ryan-Rags/site-factory/blob/1b6225ac5fe0c69ab144aa7b742bbd5ebc64d393/PLAN-prospect-parity.md) |
| `PLAN-template.md` | [blob@1b6225a](https://github.com/Ryan-Rags/site-factory/blob/1b6225ac5fe0c69ab144aa7b742bbd5ebc64d393/PLAN-template.md) |
| `PLAN-trust-seo.md` | [blob@1b6225a](https://github.com/Ryan-Rags/site-factory/blob/1b6225ac5fe0c69ab144aa7b742bbd5ebc64d393/PLAN-trust-seo.md) |
| `PR-trust-seo.md` | superseded by [PR #17](https://github.com/Ryan-Rags/site-factory/pull/17) |

`docs/evidence/**` was pruned in the same pass, per the ruling of 2026-08-12
below. Its contents are pinned in the PR bodies that cite them and remain at
[`68e2c1b`](https://github.com/Ryan-Rags/site-factory/tree/68e2c1bc823ff5276c6bd7bd75e9adac243ec666/docs/evidence)
(beta-2) and
[`1b6225a`](https://github.com/Ryan-Rags/site-factory/tree/1b6225ac5fe0c69ab144aa7b742bbd5ebc64d393/docs/evidence)
(trust-seo).

---

## Ledger

### 2026-08-06 — pipeline, template, multi-client, deploy

- **2026-08-06** — The crawl limit governs *page navigations*, and one Lighthouse run counts as one navigation; CLAUDE.md's wording was changed so this is not re-litigated. *Source: PLAN-pipeline.md Q4, PR #1.*
- **2026-08-06** — Discovery labels rows from a `--city "<label>"` flag rather than parsing a locality out of `formattedAddress`; `addressComponents` stays out of the field mask because it is a dearer Places SKU. *Source: PLAN-pipeline.md Q3, PR #1.*
- **2026-08-06** — The lead CSV keeps its seven original columns in order and appends `place_id,rating,review_count,address,discovered_at`; `data/businesses.sample.csv` is the one tracked CSV. *Source: PLAN-pipeline.md Q2, PR #1.*
- **2026-08-06** — Outreach skips any business with fewer than two confirmed findings and logs the reason to `outreach/skipped.md`; personalization is never invented to fill the gap. *Source: PLAN-pipeline.md Q7, PR #1.*
- **2026-08-06** — Without a Places key, the live path ships code-complete, typechecked and **unexecuted**, proved only against `--dry-run` fixtures. No key is ever invented and the API is not called without one. *Source: PLAN-pipeline.md Q6, PR #1.*
- **2026-08-06** — Sample rows that cannot resolve are a feature, not a gap: exercising the `unavailable` / never-fabricate path is the point of them. *Source: PLAN-pipeline.md Q5, PR #1.*
- **2026-08-06** — `VERIFY_MARKER` (`[verify with client]`) is the standard marker; `PLACEHOLDER` survives only as `LEGACY_MARKER` on the K-H config, because K-H's built output was the byte-equivalence proof. *Source: PR #3.* (**Superseded 2026-08-11** — see the marker-unification ruling below.)
- **2026-08-06** — The marker gate reads the **built bytes**, not the config: markers are fine while `seo.noindex` is `true` and fail the build the moment it is `false`, listing every file and line. Marker-freedom is a precondition of going live, not something anyone has to remember. *Source: PR #3.*
- **2026-08-06** — The client is selected by the `SITE_CLIENT` env var, not a `--client` flag, because `astro build` owns its own argv. Output goes to `dist/<slug>/` so one client's build never overwrites another's. *Source: PR #3.*
- **2026-08-06** — `business.foundedYear` is the only place a year is stated as a number, and copy states a founding year ("Since 1987") rather than an age that goes stale. *Source: PR #3.*
- **2026-08-06** — An evergreen format does not make the underlying fact true. K-H's `foundedYear` had been back-solved from prose about the owner's own time on the floor; a sourced value carries a provenance comment beside it so a later reader can check the claim instead of trusting it. *Source: PR #5, PR #6.*
- **2026-08-06** — `seo.siteUrl` and the mockup lock (`seo.noindex`) are independent and do not flip together. *Source: PR #7.*
- **2026-08-06** — `.gitignore` ignores the category, not the file: `/data/*.csv` with `data/businesses.sample.csv` as the single negation, so a new lead CSV is ignored by default rather than tracked by default. *Source: PR #8.*
- **2026-08-06** — Warn when a lead's slug matches no registered client config. Decided independently of which slug fix eventually wins, because both candidates narrow the collision class without closing it and the warning is what turns a silent, correct-looking failure into a loud one. *Source: PR #9.* (**Superseded 2026-08-12** — see placeId-first matching below.)
- **2026-08-06** — Hosting model: one Cloudflare Pages project per client, each at its own root (`<slug>-preview`), never one project with per-client subpaths — the template emits root-absolute hrefs. Verification targets the canonical `<project>.pages.dev` alias, not the per-deployment hash host. *Source: PR #10.*

### 2026-08-11 — copy, demo, demo-support, design families

- **2026-08-11** — **One marker spelling repo-wide: `[verify with client]`.** No third variant. The constant lives in `@site-factory/copy` and `src/types/site.ts` re-exports it, so there is one definition and every existing import resolves. The legacy `PLACEHOLDER` is retired from every config, and `check-markers.mjs` still refuses it so a resurrected old config cannot go live. *Source: PLAN-copy.md Q1(a), landed in PR #11.*
- **2026-08-11** — No filler copy anywhere. A marker remains only where a real question is open; a gap is reported as a gap, never smoothed over. *Source: PLAN-copy.md Q2 (Reading A), PR #11.*
- **2026-08-11** — `packages/copy` never calls Places. *Source: PLAN-copy.md Q4, PR #11.*
- **2026-08-11** — SMS components ship wired but with `smsHref` unset for every prospect: no demo shows a text link that goes nowhere, and turning one on is one line. *Source: PLAN-demo-support.md decision A, PR #11.*
- **2026-08-11** — Worker code is committed with PLACEHOLDER ids only. Nothing is deployed to Cloudflare by an agent unasked; deploying is outward-facing. *Source: PLAN-demo-support.md decision C, PR #11.*
- **2026-08-11** — A prospect's extracted `brandAccent` is offered as a swatch only where it passes the AA gate in the role it would occupy, and is **dropped, never nudged** — a shifted colour is not their colour. The gate is never relaxed. *Source: PLAN-design-families.md Q1(a), PR #11.*
- **2026-08-11** — All five hand-authored clients move to the design path; `clients/EQUIVALENCE.md` records that K-H's legacy byte-lock is superseded. *Source: PLAN-design-families.md Q2(a), PR #11.*
- **2026-08-11** — Generated configs never overwrite the five hand-authored ones. Via `SITE_CONFIG_FILE` they go further: a hand-authored config is read as the **top-precedence ingestion source**, so a demo for a business we already researched starts from that research. *Source: PLAN-demo.md Q3, PR #11.*
- **2026-08-11** — Google reviews may be shown verbatim with visible `via Google` attribution and the source recorded. Places **photos** are ingested but not published, because no component renders a visible credit near an image and there is nowhere for the attribution to go. *Source: PLAN-demo.md Q4, PR #11.*
- **2026-08-12** — `build:all` runs the same per-client gate chain as `build`. A batch build may never be a weaker gate than a single build. *Source: PR #11 (integration defect 3).*
- **2026-08-12** — Generated copy names the route ("call the shop") rather than printing the phone number as prose, since every page already renders it as a `tel:` link; `seo.ts` still emits the digits, because a meta description is read by a search result, not a thumb. *Source: PR #11 (integration defect 5).*
- **2026-08-12** — A demo pipeline that fabricates is stopped by the gate, not by review: `check-fabrication` reads a discovered prospect's own record (`prospects/<id>/prospect.json`) under the same known/unavailable discipline as the curated packs. *Source: PR #11 (integration defect 6).*

### 2026-08-12 — prospect parity (PR #12)

- **2026-08-12** — Design-family assignment follows the template's own definitions — forge = machine shops, welding, fabrication; precision = contractors, HVAC, electrical — not the prospect package's own table. `heritage` is never selected by trade: it comes from sourced legacy evidence (a sourced founding year 40+ years back, a sourced family-run claim, or a name that states it). *Source: PR #12.*
- **2026-08-12** — A colour extracted from a parked or expired-domain page is not the business's brand. `parked`/`dead` sites contribute **nothing** — no name, phone, address, services, and no logo for palette extraction — and the crawler stops after one navigation instead of spending five on a registrar. *Source: PR #12.*
- **2026-08-12** — A niche with no copy pack emits **no** FAQ and **no** service-area section and says so in the summary and `demo.json` — never a generic one. The same holds for a record too thin to write from, and a `FabricationError` discards the whole run's copy rather than publishing the sentences that happened to pass. *Source: PR #12.*
- **2026-08-12** — `GOOGLE_PLACES_API_KEY` is the documented key name; `GOOGLE_MAPS_API_KEY` is read for one release behind a once-per-process deprecation warning. *Source: PR #12.*
- **2026-08-12** — **The contrast rule has more than one implementation, and the seam stays flagged rather than silently tolerated.** `check-contrast.mjs --matrix` finds brand accents by scanning `clients/design/*.json`, so it structurally cannot see a payload passed via `SITE_CONFIG_FILE`; `packages/prospect/src/color.ts` ports the design-accent pairs so a generated demo is held to the same bar. *Source: PR #12 Brief 1.* (**Narrowed 2026-08-12** — see below.)

### 2026-08-12 — lead flow (PR #13)

- **2026-08-12** — **`KNOWN_PROSPECTS` must match the registry.** `check-form-fields.mjs` fails the build when a `PROSPECT_RECIPIENTS` key is absent from `KNOWN_PROSPECTS` (a typo'd key falls back to `MAIL_TO`: the lead survives, the routing is a lie) or when `KNOWN_PROSPECTS` is missing a slug present in `clients/index.ts`. It runs over the committed `wrangler.jsonc` and, when present, the gitignored `wrangler.local.jsonc`. *Source: PLAN-lead-flow-2.md, PR #13.*
- **2026-08-12** — **`DEMO_FORM_ENDPOINT` is the demo build's one switch.** Set, every prospect's form posts to the single shared Worker tagged with its slug — including clients whose own config says `forms.mode: 'disabled'`, which is right for a mockup and wrong for a demo whose purpose is a form that works. Unset, each client's own `forms` block governs, so **a real client build can never inherit the demo endpoint by accident**. *Source: PR #13 (introduced in PR #11).*
- **2026-08-12** — No config may produce a lead nobody can answer. The invariant is enforced three times: `resolveFields()` throws at build, the field gate fails the build, and the Worker throws illegal rules away for the defaults rather than failing open. *Source: PR #13.*
- **2026-08-12** — Visitor confirmations stay inert until `MAIL_FROM` leaves `resend.dev`, from which Resend delivers only to the account owner. No agent handles a real Resend key; Ryan flips `CONFIRMATIONS_ENABLED` and runs the live end-to-end receipt himself. Key custody is his. *Source: PLAN-lead-flow-2.md approval of 2026-08-12, PR #13.*
- **2026-08-12** — **`zz-*` fixtures never deploy.** A fixture is registered in `KNOWN_PROSPECTS` so the gates can see it, is skipped by `build-all.mjs` so it never reaches `dist/`, and is excluded by prefix in `scripts/deploy/deploy-mockups.mjs`, which announces the skip. "Never deployed" is a property of the pipeline, not a note in a README. *Source: PR #13; enforced in the deploy script by PR #15 Ruling 4.*
- **2026-08-12** — Non-default configuration is proved on a throwaway `zz-fixture-*`, never on a pitchable demo: the five prospect demos stay uniform. *Source: PLAN-lead-flow-2.md approval of 2026-08-12, PR #13.*

### 2026-08-12 — design polish (PR #14)

- **2026-08-12** — **The URL carries accent *ids*, not indices:** `?theme=heritage&scheme=dark&accent=brick&font=old-serif`. An index would renumber the moment swatches are added, silently changing what every previously-shared link resolves to. *Source: PLAN-design-polish.md Q1, PR #14.*
- **2026-08-12** — **Curated swatches hard-fail; only a prospect's `brandAccent` auto-drops, and it drops per cell.** A curated swatch that fails its cell is a build failure — they are our colours, and silently dropping one converts a loud, fixable authoring error into a swatch that quietly vanishes from a scheme. A brand accent legal on a light Heritage and illegal on a dark Forge is offered where it works instead of everywhere or nowhere. One mechanism (`offeredCells()`) feeds the gate, the CSS and the UI, and every drop is printed by name and cell. *Source: PLAN-design-polish.md Q2, PR #14.*
- **2026-08-12** — **`theme.scheme` defaults to the tone each family already shipped in, and there is no `prefers-color-scheme` auto-follow.** The demo shows a deliberate design decision; a prospect's OS preference silently flipping it on open would read as exactly the bug being fixed. The config default wins and the toggle is explicit. Both tones carry the same accent *ids* with different values, so flipping the tone never reassigns a prospect's colour. *Source: PLAN-design-polish.md Q3, PR #14.*
- **2026-08-12** — `offeredPresets()` is the single source of which theme cells exist; the matrix emitter, the panel's controls, the panel's resolver and the pre-paint script all derive from it, and one `resolve()` clamps any request — URL, `localStorage` or click — to a legal cell. The pre-paint script resolves rather than obeys, and a URL that carried a selection is rewritten to name what is actually on screen. *Source: PR #14.*
- **2026-08-12** — Metric tokens stay on `:root[data-theme]` at the lowest specificity so no combination can lose them, which is what keeps a tone flip free of layout shift. *Source: PR #14.*

### 2026-08-12 — beta-2 verify-and-repair (PR #15)

- **2026-08-12** — **The fabrication boundary is a union, not a substitution.** When a slug owns *both* an ingested prospect record and a curated copy pack, `check-fabrication.mjs` checks against the **union of both allowance sets**. Replace semantics are kept for a purely generated prospect, and a hard error naming both records is kept for neither. No allowance moves between slugs, and content precedence is untouched — the hand-authored config still wins for what ships; the union applies only to the allowance set this gate consults. Landed failure-first (`dc138d5` red, `704a8ed` green). *Source: PR #15 §1.3 / Brief 1.*
- **2026-08-12** — `color-scheme` is emitted on **delivered** builds, not only in the pitch matrix: a delivered dark site otherwise gets a light scrollbar. *Source: PR #15 Ruling 1.*
- **2026-08-12** — **The parity gate takes a narrow named allowance, never a re-baseline.** A delivered `:root` block may differ *only* by the ADDITION of a single `color-scheme: light|dark;`, and only where the baseline had none; every page taking the exemption is printed by name. One other token moving alongside it, or the scheme value flipping once present, still fails. Re-baselining was rejected because it would absorb unrelated drift landing in the same build, and keep absorbing it. *Source: PR #15 Ruling 1.*
- **2026-08-12** — `color-scheme` is design-pages-only. The legacy `BaseLayout` pages (`/about`, `/services`, `/contact`, `/404`) carry no design tokens and no resolved scheme, so a client's home page declares its tone while its contact page declares nothing. Correct as built; a named seam to settle before the legacy pages get a dark treatment. *Source: PR #15 Brief 2 (open).*
- **2026-08-12** — A lead-row miss is a structural `warnings` channel — not string-sniffing the log — carried into the manifest and printed under its own banner at the **end** of the summary and on **stderr**, so a piped run cannot lose it. *Source: PR #15 Ruling 3.*
- **2026-08-12** — **The contrast seam is a double, not a triple.** `contrast.mjs` is canonical; `packages/prospect/src/color.ts`'s 17 pairs, derivations, thresholds and `1e-9` slack are equivalent to it and were never stale — only the *shape* they were fed was wrong. Unification stays a named follow-up rather than a rediscovery. *Source: PR #15 §1.1 and Brief 4 (open), narrowing PR #12 Brief 1.*
- **2026-08-12** — **Evidence images are welcome during review and are deleted by the next cleanup.** A PR body that cites evidence pins every image and link to a SHA rather than to a branch, so it keeps rendering once the files leave `HEAD`. *Source: PR #15 Brief 5.*

### 2026-08-12 — discovery (PR #16)

- **2026-08-12** — **One Enterprise sweep, no second pass.** `places.websiteUri` and `places.nationalPhoneNumber` are Enterprise, not Pro, which killed the two-pass survivor design: the filter was defined on exactly those fields, so a Pro sweep would have had nothing to filter on and Details would have had to run against every deduplicated business in the county. Once the call is Enterprise, `rating` and `userRatingCount` ride along at no marginal cost. 210 calls for a 70-town × 3-niche county sweep. *Source: PLAN-discovery.md addendum, PR #16.*
- **2026-08-12** — **The Enterprise + Atmosphere SKU class is banned outright**, enforced at the call site rather than asserted about a constant. The sweep mask must equal the declared mask byte-for-byte, every field must have a known price, and an unpriced field resolves to the dearest tier so it is a gate failure rather than a silent cost. The tier table carries a VERIFY-BEFORE-A-LIVE-RUN header and `checkSkuTable()` guards its internal consistency. *Source: PR #16; re-affirmed by the county dry run in PR #18's stream.*
- **2026-08-12** — **placeId-first matching supersedes the slug WARNING.** A Places id is assigned by Google, not derived from a name, so it survives a legal suffix, a punctuation change or a rename — which is the entire collision class filed in `PLAN-pipeline.md`'s backlog. The slug match remains as a **loud fallback** for records predating place ids, which is what that backlog entry itself asked for. *Source: PR #16, closing the interim of PR #9.*
- **2026-08-12** — `--backfill-place-ids` is opt-in, prints its plan before acting, and only ever writes the `place_id` cell of a row whose id is empty. A wrong place id silently attaches one business's identity to another; an empty one merely falls back to the loud warning. *Source: PR #16.*
- **2026-08-12** — Call-list reasons rank by **salience, not contribution**: a missing component contributes zero, which buried "no phone number — cannot be called" beneath "copy pack ready", so a missing component now competes on the weight it **forfeited**, ties breaking toward the negative. On a call list a fatal gap is the most decision-relevant thing a row can say. *Source: PR #16 Brief 4.* (**Extended 2026-08-12** — see below.)
- **2026-08-12** — Audit-ordering weights are ordinal, not calibrated, and are left uncalibrated until a live run shows whether the queue surfaces the worst sites first. They never enter the score; they only sort the audit queue, and the whole table prints in the run header. *Source: PR #16 Brief 2.*
- **2026-08-12** — The live sweep and its console reconciliation are Ryan's to run. Everything is proved against fixtures; no agent handles the key. *Source: PLAN-discovery.md Q5, PR #16.*

### 2026-08-12 — trust & SEO (PR #17)

- **2026-08-12** — **The design payload wins for `areaServed`.** Within one build, the page's rendered service areas and the JSON-LD `areaServed` derive from a **single** source and can never disagree: when a `design` block exists that source is the design payload, otherwise it is `site.serviceAreas`. `ServiceArea.astro`'s own fallback to `business.serviceArea` on an empty list is mirrored so the two cannot diverge. The lint asserts consistency between the two representations **site-wide**, not prose-subset per page. *Source: PR #17 defect 2.*
- **2026-08-12** — `script-src` is hash-only and permanently asserted: `'unsafe-inline'`, `'unsafe-eval'`, `'strict-dynamic'`, `'unsafe-hashes'` and wildcards are refused there, and `'unsafe-inline'` is refused in every directive but `style-src`. *Source: PLAN-trust-seo.md ruling on `style-src`, PR #17.*
- **2026-08-12** — **Turnstile is blocked from being enabled for any client** until the hydration race is fixed. `contact.astro` loads `api.js` at page level while `.cf-turnstile` lives inside a `client:visible` React island, so roughly one run in five React wipes the widget and the form posts with no `cf-turnstile-response` token — a submission any verifying Worker rejects. The fix is `?render=explicit` plus `turnstile.render()` from a `useEffect`, which removes the race rather than narrowing it. The test key was removed from the fixture rather than importing a one-in-five third-party flake into a gate that runs on every `build:all`; the CSP path is proved by recorded measurement and a reproducible command, and the key is re-added the moment the race is fixed, at which point it becomes a permanent regression test for both. *Source: PR #17 Brief 1 and 2; repro in `docs/known-issues.md`.*
- **2026-08-12** — No HSTS in `_headers`: it belongs to the zone, not to a project that also answers on `*.pages.dev`. The go-live runbook covers it. *Source: PR #17.*
- **2026-08-12** — A social card must be a real raster of the declared size. Facebook, X, LinkedIn, iMessage, WhatsApp and Slack all decline to render an SVG `og:image`, so an SVG card means every shared link arrives with no image. *Source: PR #17 defect 1.*
- **2026-08-12** — A gate fails **closed** on any shape it cannot follow. A narrow trigger is worse than a narrow match, because skipping produces no output at all. *Source: PR #17 ("three fail-opens in my own gates").*
- **2026-08-12** — `example.invalid` may remain in a config's `siteUrl` while that client is noindex; `check-go-live.mjs` is the control that refuses to let it go live carrying one. *Source: PR #17 Brief 4.*

### 2026-08-12 — served-fixture exercise (PR #18)

- **2026-08-12** — **Forfeited-weight reason semantics apply to every scoring component, opportunity included.** Opportunity previously always carried `forfeited: 0`, so an unaudited live site read as three positives with no hint its site was never measured — the score was right and the sentence was a lie. Opportunity now forfeits what it did not claim, the same rule as the viability components; because its bands are 0.10–0.65 and 0.90–1.00, `max(value, 1 − value)` never falls below 0.65, so **the website's condition is always the headline**. Never scored as decent by default; unmeasured is unavailable. *Source: PR #18, extending PR #16 Brief 4.*
- **2026-08-12** — Test fixtures are hand-written, never a saved copy of a real business's page, because CLAUDE.md forbids committing third-party business data. Served fixtures get one loopback IP each, since `NavigationBudget` keys on hostname and several sites behind one host would share one budget and one throttle — an artefact of the test rather than of the system. *Source: PR #18.*

### 2026-08-12 — docs cleanup (this PR)

- **2026-08-12** — A stream's `PLAN-<stream>.md` lives on its branch and is deleted by the PR that completes the stream; durable rulings are appended to this file in that same PR. Sessions report plan and PR locations as GitHub blob URLs, never editor-internal links. *Source: CLAUDE.md "Plan files & docs", this PR.*

### 2026-08-12 — loose ends (PR #21)

- **2026-08-12** — **The repo root carries no plan documents, `PLAN.md` included.** This **reverses** the "Where the sources went" table above, which recorded `PLAN.md` as *kept — repo scaffold notes, not a stream plan*: the exemption reintroduced exactly the split the cleanup closed, one plan doc at the root and every ruling here. Its two scaffold decisions are restated as the two lines below, and the file itself remains permanently readable at [blob@bfee8bc](https://github.com/Ryan-Rags/site-factory/blob/bfee8bcc79cf15511c7afc66cd4fcd0836de4919/PLAN.md). *Source: PR #20 Brief 2, ruled; landed in PR #21.*
- **2026-08-12** — Package directories live under `packages/`, never at the repo root — the idiomatic pnpm layout, and it keeps `data/`, `docs/` and future infra directories unambiguous. *Source: [`PLAN.md`@bfee8bc](https://github.com/Ryan-Rags/site-factory/blob/bfee8bcc79cf15511c7afc66cd4fcd0836de4919/PLAN.md) "Decisions made during scaffold", carried here by PR #21.*
- **2026-08-12** — Each package carries its own `tsconfig.json` and `typescript` devDependency, so `pnpm -r build` resolves without relying on root hoisting. *Source: [`PLAN.md`@bfee8bc](https://github.com/Ryan-Rags/site-factory/blob/bfee8bcc79cf15511c7afc66cd4fcd0836de4919/PLAN.md) "Decisions made during scaffold", carried here by PR #21.*
- **2026-08-12** — **`freePort` has one definition, in `@site-factory/audit`.** The port and the Lighthouse run that consumes it belong to the same package; `packages/shortlist` and its e2e test import it rather than keeping copies, and `packages/shortlist`'s index does not re-export it. *Source: PR #19 Brief, ruled; landed in PR #21.*
- **2026-08-12** — **`AssessOptions.debugPort` is required and has no default.** A browser launched without `--remote-debugging-port` opens no port at all, so any default is a port nobody is listening on — which is precisely the silent degradation PR #19 fixed by wiring. Requiring it turns that class into a compile error rather than a convention. *Source: PR #19's flagged not-done, ruled; landed in PR #21.*

### 2026-08-12 — live smoke (PR #22)

- **2026-08-12** — **Post-deploy verification derives every expectation from the live response, never from local `dist/`.** The deployed artifact and the local build are demonstrably different things: the live `kh-machine-works` declared `og:image = https://www.khmachineworks.com/og/kh-machine-works.png` while the same client in the checkout declared `…/images/og.svg`. A check that took its expectation from `dist/` would be grading a build nobody is serving and could report green on a deploy nobody has looked at. So the CSP is re-derived from the live pages, the route list is read from the live nav, the form endpoint is read from the live contact page, and the card is parsed from the bytes the live URL returns. *Source: PLAN-live-smoke.md §1, PR #22.*
- **2026-08-12** — **A registered client whose deploy does not answer is a failure, reported as its own findings class (`not-deployed`).** "The client we thought was live isn't" is the class the suite exists to surface, and separating it from a broken deploy is what keeps the two from being read as the same problem. *Source: PLAN-live-smoke.md Brief 1, approved 2026-08-12; PR #22.*
- **2026-08-12** — **Lighthouse `seo` is reported and never enforced while every demo is `noindex`.** The `is-crawlable` deduction is the demo lock working, so a bar there would fail every client for being correctly configured. The bar arrives when a go-live smoke exists, where 100/100/100/100 is already demonstrated on the go-live fixture. *Source: PLAN-live-smoke.md Brief 2, approved 2026-08-12; PR #22.*
- **2026-08-12** — **A security header present on a deploy that carries none of this repo's generated headers is labelled `platform (inferred: not produced by this repo)` and is never counted as ours.** Cloudflare Pages supplies `X-Content-Type-Options` and `Referrer-Policy` on `*.pages.dev` by itself, so a check asserting only the required-header map scores a CSP-less site 2/4 and reads as "mostly fine" — "green ≠ correct" reproducing inside the tool built to prevent it. Provenance is decided by whether any header only `gen-headers.mjs` produces is present. *Source: PLAN-live-smoke.md Brief 4, approved 2026-08-12; PR #22.*
- **2026-08-12** — **The live form path is exercised by one honeypot POST per client per run, hard-capped in code.** `worker-demo` checks the honeypot before the prospect-id gate, the rate limiter, the KV write and Resend, so the request proves method gating, origin gating, CORS and reachability while creating no lead, no email and no rate-limit consumption. No name, phone, email, message or file is ever sent. It covers the shared **demo** Worker only — never the single-tenant `worker/` — and the report says so in its header. *Source: PLAN-live-smoke.md §3.6 and Brief 3, approved 2026-08-12; PR #22.*
- **2026-08-12** — **A known issue never fails smoke; an unknown one always does — so the rule discriminates by LHR signature, not by category name.** `runLighthouse` now returns the raw LHR for that purpose, and the waiver is refused outright if `docs/known-issues.md` stops carrying the entry that grants it. A waiver outliving its issue is how a suite goes quietly blind. *Source: PLAN-live-smoke.md §3.8, PR #22.*
- **2026-08-12** — **Live smoke spaces bare probes at 500 ms and page navigations at no less than 1000 ms per domain.** PLAN-live-smoke §2 set a single 500 ms interval; claude.md's limit names *navigations* specifically, so the two tiers are kept apart rather than the looser number being applied to both. Probes do not consume navigation budget, which stays capped at 10 per site. *Source: PR #22 Brief 5.*

### 2026-08-12 — NO_LCP, measured (PR #24)

- **2026-08-12** — **A waiver widens on measurement, never on inference — and the widening carries the fingerprint that was measured, not the conclusion it suggested.** The `NO_LCP` waiver is extended to accept `total-blocking-time` unscored **only in an LHR whose LCP audit also carries the `NO_LCP` error message**. `NO_LCP` from any other cause, `total-blocking-time` unscored on its own, a third unscored weighted performance audit, and a `docs/known-issues.md` that no longer carries a `NO_LCP` entry are each still refused. Extending on "both are lantern metrics, so it is probably one cause" would have been reasoning from the shape of the inconvenience; measuring the pair five-for-five on the live fleet is what actually authorises it. **This ruling authorises the change; it does not make it** — the code is a gate-script edit and lands separately. *Source: Brief 3 ruling of 2026-08-12, PR #24; measurement in `docs/known-issues.md` #2 addendum against [`report.md`@ef98f75](https://github.com/Ryan-Rags/site-factory/blob/ef98f7532ab24f3b9f721670cf6b1fe8fe5b5909/docs/evidence/live-smoke/report.md).*
- **2026-08-12** — **The 90+ mobile Lighthouse bar is demonstrated on `DesignLayout`** — 100 / 99 / 98 on `american-machine-specialty`, `industrial-machine-corp` and `kh-machine-works`, live, under the same mobile emulation and Lighthouse 12.8.2 that yields `NO_LCP` on the other five. `NO_LCP` is therefore a **page-specific** defect, and no claim may treat it as a property of the layout or of mobile emulation as such. *Source: PR #24, same measurement.*
- **2026-08-12** — **`docs/known-issues.md` entry numbers are never reused and never renumbered.** A fixed entry is deleted and its number left as a gap, because `scripts/live-smoke` and `packages/audit` cite entries by number in code and comments, and renumbering silently repoints those citations at the wrong defect. *Source: PR #24, on deleting entry #3.*
- **2026-08-12** — **Known-issues #3 (`packages/shortlist` opened no debugging port) is closed.** `launchAuditBrowser` allocates via `@site-factory/audit`'s `freePort` and launches with `--remote-debugging-port`, `AssessOptions.debugPort` is required with no default, and `packages/shortlist/test/launcher.test.mjs` fails a launch that omits the flag. *Source: PR #19 and PR #21, recorded closed by PR #24.*

### 2026-08-13 — design coverage (PR #32)

- **2026-08-13** — **A business name is never shortened to fit the furniture; the furniture gives way first.** The header lockup escalates — fluid size, then a tracking reduction that cannot loosen an already-negative tracking, then a second line — and stops there. No `text-overflow`, no clamp, no hidden overflow. A name too long for two lines fails `check:textfit` loudly rather than losing characters silently, which is why the escalation is allowed to end without a truncation step. `zz-fixture-long-name` is the standing regression case and exists so the measured budget cannot be narrowed by accident. *Source: PLAN-design-coverage.md item 3, PR #32.*
- **2026-08-13** — **Off-home in-page anchors resolve to the home page** (`#services` on `/contact` becomes `/#services`), rather than the nav being repointed at the real routes. Labels stay identical across every route, every link resolves, and home's markup does not move. Repointing the nav is a different site structure and a bigger decision than a coverage stream makes on its own. `resolveHref()` is the identity function on `/`, which is the property that keeps eight shipped home pages byte-identical. *Source: PLAN-design-coverage.md item 2 / Brief 2, PR #32.*
- **2026-08-13** — **Nothing inside the first screen may be pre-hidden by the reveal decoration**, and the check is made at `DOMContentLoaded` because that is the window in which the damage is done. The geometry is left to the IntersectionObserver rather than read with `getBoundingClientRect()`: `data-reveal-ready` is set inside the observer's first callback, so by the time anything can be hidden everything visible is already revealed, and a page that never gets a callback hides nothing at all. Measuring instead cost 48.7ms of forced reflow, which is a real regression traded for a real fix. *Source: PLAN-design-coverage.md item 5 / rulings #7 and #8, PR #32.*
- **2026-08-13** — **A parity allowance is named, is printed with every page that takes it, and expires by itself where it can.** `redesignedRoute` applies only where the *baseline* page is legacy and the candidate is a design page, so it stops applying the moment this lands on main. `/` is never eligible for it. An allowance nobody can see is indistinguishable from a gate that stopped working. *Source: PLAN-design-coverage.md item 1, PR #32.*
- **2026-08-13** — **`/gallery` is proved on a fixture, and no client's `features.gallery` flag is touched.** Turning the gallery on for a real shop needs real photographs of that shop's own work; a design client shown six stock placeholders captioned as recent work is being shown fabricated work. The design `/gallery` route therefore reads `design.sections.gallery.items` and treats an empty array as a build error naming the field, rather than falling back to the legacy route's stock SVGs. *Source: PLAN-design-coverage.md Brief 6, PR #32.*
- **2026-08-13** — **A fixture's `(test data)` marker moves off `business.name` when the name is the thing under measurement**, and onto `legalName` — which the footer prints on every page — plus a 555-01xx number, an invented town and `example.invalid`. Twelve characters of marker on the one string a lockup fixture exists to measure would push it past the bound and make the fixture fail the gate it defends. The marker is not dropped, it is relocated. *Source: PLAN-design-coverage.md Brief 5, PR #32.*


### 2026-08-13 — client onboarding intake (PR #39)

- **2026-08-13** — **A client's own answer on the intake call is a primary source, and is recorded as such: `fact(value, 'client call, <date>')`.** They are the authority on their own business, so an answer they give is publishable — but only once the *fact that they said it* is written down. An answer we do not get stays `unconfirmed()` and therefore `[verify with client]`, which is fine on a mockup and refuses the live build. There is no third state where a plausible value is filled in pending confirmation. *Source: `docs/client-onboarding.md`, PR #39.*
- **2026-08-13** — **`docs/client-onboarding.md` names no plan tier and no price, because the repo defines neither.** The plan table ships with its rows blank and an HTML comment saying who fills them, rather than a plausible Starter/Pro/Premium invented to make the section read finished. A fabricated tier in the one document whose subject is the fabrication guard would be the guard defeated at its own source. The blank is filled once, by Ryan, in a later edit. *Source: PR #39 Brief 1.*
- **2026-08-13** — **"Family-owned" is asked as three questions, never one** — owned by a family today, the *same* family that founded it, and which generation. A shop founded by one family and bought by another is family-owned and is not "family-owned since 1958", and only the second phrasing is the one that reaches a hero. Ownership class is additionally legally meaningful for set-aside programmes, so it is always sourced and never inferred from a surname over the door. *Source: `docs/client-onboarding.md` §1, PR #39.*

### 2026-08-13 — launch blockers (PR #38, completed by PR #40)

*The code for this stream was squash-merged as [PR #38](https://github.com/Ryan-Rags/site-factory/pull/38) from `fix/launch-blockers` at commit `bfacebc`, before the stream had opened its own PR. PR #40 carries the remainder: the whole-tree fix to `check:parity`, these entries, the tombstone number, and the plan-file deletion. Rulings below cite the PR that actually carries each one.*

- **2026-08-13** — **The social card is the one absolute URL rooted at the origin actually serving the build; every other absolute URL is an identity claim and keeps `seo.siteUrl`.** `og:image` and `twitter:image` resolve against `cardOrigin`; the canonical and `og:url` deliberately do not move, because pointing those at a `pages.dev` subdomain would tell crawlers the prospect's business lives there — a worse change than the broken image it would fix. The trigger is `site.seo.noindex`, the flag that already locks a mockup and is already read back off built HTML by `check-metadata.mjs`, and **not** a new env var: an unset variable would silently reproduce the defect on the next `build:all`. `cardOrigin` is `''` when `noindex` is false, so a delivered build is byte-identical. *Source: PLAN-launch-blockers.md item 1, issue #25; PR #38.*
- **2026-08-13** — **A parity allowance for a host migration is one-way: it is refused if the baseline is already at the destination.** `previewCardOrigin` requires the baseline origin to differ from `https://<slug>-preview.pages.dev`, requires both tags to land on exactly that origin, and requires each pathname to be byte-identical — so it licenses the migration once and never becomes a standing licence for the card's host to drift afterwards. *Source: PLAN-launch-blockers.md item 5; PR #38, with the whole-tree slug fix in PR #40.*
- **2026-08-13** — **A nav or footer entry whose in-page anchor names a section the build does not render is dropped, rather than the section being forced on or the label being left dead.** The anchor→section map is derived from the section components' own ids rather than hand-written beside them, and the drop is applied in the desktop nav, the `<details>` panel nav and the footer — PR #32 records those navs drifting apart once already. Seven clients drop nothing and stay byte-identical; `industrial-machine-corp` drops `Reviews → #reviews`, which is dead because `testimonials: []` is deliberate. *Source: PLAN-launch-blockers.md item 2a, PR #32 Brief 1, issue #33; PR #38.*
- **2026-08-13** — **Sideways scroll is fixed in the layout that causes it, never by widening `overflow-x: hidden` and never by editing the client's config out of the variant.** `.d-services__grid--list .d-service` was `flex-direction: row` over five siblings, so the card laid out to 510px and `/` scrolled 190px sideways at 320. The fix states the intended two-column grid with `minmax(0, 1fr)` on the copy column; hiding the overflow would have hidden the measurement instead of the defect, and moving the client off `services.variant: "list"` would have left the variant broken for the next client to choose it. *Source: PLAN-launch-blockers.md item 2b; PR #38.*
- **2026-08-13** — **Dismissing the customizer is not a customization: it closes the panel and mutates nothing.** The outside-click `pointerdown` listener never calls `apply()` or `persist()`, never calls `preventDefault()`, and leaves `data-theme`, `data-scheme`, `data-accent`, `data-font` and the query string exactly as they were. A dismissal that silently committed the panel's current state would make a stray click a permanent change the visitor never asked for. *Source: PLAN-launch-blockers.md item 3; PR #38.*
- **2026-08-13** — **Every browser gate verifies which build the server is actually serving before it measures anything.** Ports move between sessions and a gate pointed at a stale preview grades a build nobody changed — green on work that was never measured, red on work that was never done. This is the third recorded occurrence, so it is a rule rather than a habit: a gate that navigates to a `PREVIEW_URL` reads the served slug off the build's own markup and exits non-zero on a mismatch, before its first assertion. A gate that serves its own `dist/<slug>/` on an ephemeral port satisfies this structurally and needs no check. *Source: Brief 4 ruling of 2026-08-13; audit and backfill tracked as issue #37; PR #40.*
- **2026-08-13** — **A `known-issues.md` number that was filed twice retires both numbers.** The 2026-08-12 rule that numbers are never reused or renumbered extends to a duplicate: the og:image defect was filed as `## 4` by PR #22 and again as `## 4` by PR #23, the duplicate should have been `#5`, and deleting the entry retires 4 and 5 together rather than renumbering a live entry to reclaim one. The next entry filed is `#6`. *Source: PLAN-launch-blockers.md item 4, ruled 2026-08-13; PR #40.*

### 2026-08-13 — design expansion: two families, and the motion axis (PR #44)

- **2026-08-13** — **A design family is a palette *and* a cosmetic block; a palette-only family is an accent set.** `meridian` and `apex` each ship light + dark, their own accents, their own pairings and their own `[data-theme]` rules in `design.css` alongside forge's badges, precision's corner ticks and heritage's double rule. The customizer already offers eight accents per tone, so a family that changed only colours would be indistinguishable from picking a different swatch. *Source: PLAN-design-expansion.md item 1; Brief 3 of PR #32.*
- **2026-08-13** — **Apex is "one electric accent" as a matter of character, not cardinality: it ships eight, all in the electric register.** A family whose accent group had nothing to switch between would be the only one in the matrix like that. *Source: PLAN-design-expansion.md Brief 1, ruled 2026-08-13.*
- **2026-08-13** — **A curated accent is solved against the gate, not eyeballed: maximum chroma at the lightness the contrast rule forces, inside the family's own measured register.** `heritage/dark/oxblood` was `#e5808f`, a pale pink at OKLCH chroma 0.125 while the gate permitted 0.184 at that lightness — so the pink was a picking artefact and not a mathematical necessity, exactly as the plan predicted. Re-picked to `#e05366`, deeper and 40% more chromatic. The per-family chroma and lightness bands are read off the seven accents each family already shipped, so an addition sits inside its family instead of shouting over it. *Source: PLAN-design-expansion.md items 2 and 7(a).*
- **2026-08-13** — **The motion axis is four custom properties whose CSS fallbacks are the literals that shipped, and the stagger is a multiplier on the inline `--d-reveal-delay`, never an edit to it.** Eight components emit that delay as inline style inside the region `check-delivered-parity.mjs` compares, so replacing those literals would move every delivered page; at the default `1` the arithmetic is the identity. Absent attribute → fallbacks → today's exact behaviour. *Source: PLAN-design-expansion.md items 5 and §0.*
- **2026-08-13** — **The page-level motion attribute is `data-motion-preset`, not `data-motion`, because `data-motion` was already taken.** `DesignHero.astro` uses `data-motion="pan|zoom"` on hero images. Those rules are scoped to `img` so there is no CSS collision today, but the rename is what lets the orthogonality assertion be exact rather than an assertion carrying an exception. *Source: PLAN-design-expansion.md item 5, corrected during implementation.*
- **2026-08-13** — **Orthogonality between the motion axis and the theme matrix is proved statically, and that proof is what licenses the reveal gate to run as a SUM.** No emitted selector may combine `data-motion-preset` with `data-theme`, `data-scheme`, `data-accent` or `data-font`; `check-reveal.mjs` scans the served page's own CSS and exits non-zero before launching any sweep. The motion pass is then three presets over one representative route set rather than a product with the 272-cell matrix. Demonstrated failing first with a deliberately combined selector. *Source: PLAN-design-expansion.md item 3.3.*
- **2026-08-13** — **A gate's settle window comes from the declared contract, and a preset that declares none fails rather than inheriting a default.** `check-reveal.mjs` reads `motion.reveal.settle` off the served page's preset. **The plan's stated reason for this was wrong, and measurement corrected it:** calm's 900ms did *not* break the unmodified 800ms gate, because opacity is read only after a second full settle and any revealed element therefore has ~1.75s to finish. The old literal was not incorrect — it was not doing the job its comment claimed, and correctness resting on an accident of the gate's own scroll pattern is how a suite goes blind. *Source: PLAN-design-expansion.md items 3.2 and 4; evidence in `docs/evidence/reveal-settle-window.md`.*
- **2026-08-13** — **A gate may not name a cause it cannot distinguish.** The reveal gate reported elements "being re-hidden behind the reader" 155 times against an 8s transition when every one of them was still rising, at opacity 0.996 — the same misdiagnosis its own header records having shipped once before. An element below opacity 1 now gets a second read before the failure is worded. *Source: `docs/evidence/reveal-settle-window.md`, run 3.*
- **2026-08-13** — **A narrowed parity exemption does not forbid a new one; it forbids an *unnamed* one.** `onlyRevealScriptChanged` was pinned to its own migration via the `primed` token, and the motion work then came back through its own one-way allowance, `revealMotionAxis`, pinned on `data-motion-preset`. Both expire the moment this branch lands on main, because a later baseline carries the token and the predicate returns null. The alternative — a second copy of a 250-line inline script differing by two lines — is a worse thing to own than a narrow, announced, self-expiring allowance. *Source: PLAN-design-expansion.md item 3.1 and Brief 3; evidence in `docs/evidence/reveal-parity-exemption.md`.*
- **2026-08-13** — **"Unclipped" is not the bar for a CTA in fixed chrome; one line is.** A wrapped "Get in touch" is unclipped and passed the old assertion. Extending it found two pre-existing defects: the sticky call bar's label breaking `Call (555) 010-0177` across two lines in 44 cells — at 320px in all five families — and the header CTA wrapping in heritage's widest face at 1024px in both tones. **The plan named the wrong element for the first**: `.d-callbar__call` is a composite that is deliberately two lines, so the assertion targets the text runs inside it (`.d-callbar__label`, `.d-callbar__note`) rather than the button. *Source: PLAN-design-expansion.md item 7(b), corrected by measurement.*
- **2026-08-13** — **A gate's "widest cell" is derived, never named.** `check-textfit.mjs` hardcoded `heritage/light/signwriter`, which was true when three families offered eight pairings and is a guess once five offer seventeen. It now renders the client's own business name in every offered pairing, at the weight, transform and tracking that pairing declares, and keeps the widest. A stale "widest" is a route sweep that faithfully proves the wrong thing and reports green. *Source: PLAN-design-expansion.md item 7(b).*
- **2026-08-13** — **Comments inside an `is:inline` script are shipped bytes.** The first draft of the motion comment in `Reveal.astro` added 588 B to every one of the five pages of all three delivered design clients. Explanations belong in the component's frontmatter, which is build-time only and costs nothing. *Source: measured during PLAN-design-expansion.md item 8(c).*
- **2026-08-13** — **The comparison page is one pitch build loaded once per family with `?theme=`, not one client directory per family.** `ks-welding-forge`, `-precision` and `-heritage` are one real business occupying three client slots, and two more families would have meant two more. The page now fails loudly if generated against a build without the matrix, because otherwise it renders identical panes under different labels and still looks like a comparison. The three existing directories are left untouched — they are delivered output, and removing them is a separate ruling. *Source: PLAN-design-expansion.md item 8(a) and Brief 4.*
- **2026-08-13** — **A new URL query parameter is admitted through `check-injection.mjs`, not around it.** `motion` was the fifth; the gate failed the build until the resolver was audited and `ALLOWED_PARAMS` widened deliberately, with the audit written into the gate and into `docs/injection-surface.md`. Every source — URL, `localStorage`, config — lands in one `indexOf` against the flat motion id list, so only the three ids can reach `setAttribute`. *Source: PLAN-design-expansion.md item 5; `docs/injection-surface.md` §2.*
- **2026-08-13** — **The inlined-CSS growth from two extra families is reported and deliberately not compacted.** Measured against a pre-edit baseline: a delivered design client's home page grows 3,046 B raw / **586 B gzipped**; a customizer build grows 32,037 B raw / **4,196 B gzipped**. Per-build family emission would recover only the delivered figure, would change delivered bytes, and would forfeit the byte-identity proof — so it is proposed as its own ruling and not done here. On these numbers it is not obviously worth doing. *Source: PLAN-design-expansion.md item 8(c) and Brief 5.*
- **2026-08-13** — **Holding the phone number on one line costs bar width, and the chrome pays it, not the type size and not the client's copy.** `white-space: nowrap` on `.d-callbar__label` fixed the wrap and pushed `.d-callbar__secondary` 43px off the right edge at 320px on `ks-welding` — `check:overflow` red on all five routes where `main` is green, with the CTA clipped rather than the page scrolled. Shrinking the number to fit needs ~12px, and `callLabel` and the secondary's text are the client's own config, so below 360px the bar drops the phone glyph and tightens its padding instead: 283px of content in 304px of bar, 21px clear. The button still reads "Call (201) 385-8848", so what is given up is decoration. *Source: PLAN-design-expansion.md item 7(b); evidence in `docs/evidence/callbar-single-line.md` §2–3; PR #44.*
- **2026-08-13** — **The gate that motivates a fix does not validate it.** `check:textfit` cannot see a button pushed past the viewport — it is still one line — so the fix it asked for read green on the gate that asked for it and red on a gate nobody had re-run. This is the argument for the standing rule that the FULL suite runs on the merged state rather than the gates a change appears to touch: the regression was found on a re-run, one commit before a PR that would have claimed byte-parity and a clean sweep. *Source: `docs/evidence/callbar-single-line.md` §2; PR #44.*

### 2026-08-14 — ops: core-eight redeploy and the navigation ledger (PR #48)

- **2026-08-14** — **A session may run a live Places sweep itself, reading `GOOGLE_PLACES_API_KEY` from `.env`.** This narrows the earlier live-run custody rule, which reserved every keyed run for Ryan. The key's own status is unchanged: it never appears in chat, in a commit, in a log, or in a report, and nothing echoes its value. What is amended is only who may spend calls against it. *Source: ruling given in the ops session of 2026-08-14; PR #48.*
- **2026-08-14** — **The live-smoke navigation ledger is load-bearing, and a design family added without moving it costs a whole check.** Ten navigations per site is claude.md's limit, and the ledger in `browser.mjs` is how the suite stays inside it. #44 took the families from three to five; the customizer's sample grew from four navigations to six and the ledger did not move, so all five customizer-carrying demos spent the tenth navigation and threw on the eleventh — and because the throw left `run()`, it discarded the twenty-five assertions each had already measured. The `/about/` board shot is dropped to pay for the two, and the customizer now reports the cells it measured instead of throwing, so a stale ledger surfaces as a `suite-budget` finding rather than as a lost run. `/about/` is still fetched and asserted by the routes check; only its screenshot is gone. *Source: measured on the live core-eight run of 2026-08-14; PR #48.*
- **2026-08-14** — **A throw that crosses a stage boundary discards what was already paid for, and that is a defect wherever it appears.** The customizer lost twenty-five assertions to a navigation ceiling; the county sweep lost seventy towns of Places results to a call ceiling, because `sweep()` rethrows on budget exhaustion and `cli.ts` writes its CSV only after `sweep()` returns. Both spend a finite, metered resource before the throw. The rule is that the ceiling stops further spending and never destroys the results already bought. Fixed here for the customizer; recorded as open for the sweep in `docs/known-issues.md`. *Source: measured on the live sweep of 2026-08-14; PR #48.*
