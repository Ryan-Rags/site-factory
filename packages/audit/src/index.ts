/**
 * @site-factory/audit — scores a discovered business's existing web presence.
 *
 * Read-only GETs, max 1 request/sec/domain, max 10 pages/site. Checks that
 * cannot be completed are reported as "unavailable"; audit data is never
 * fabricated or inferred.
 *
 * Skeleton only — no implementation yet.
 */

export const PACKAGE_NAME = "@site-factory/audit";
