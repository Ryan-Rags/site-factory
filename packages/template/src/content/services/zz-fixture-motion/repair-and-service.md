---
title: Repair and service
summary: A second invented service, so the services route renders more than one section.
highlights:
  - A second entry, for the same schema reason as the first
  - Describes no real capability
  - Belongs to a test fixture, not a client
---

Also not real. The second entry keeps the `/services` route rendering more than
one section, so the staggered reveal on `.d-service` cards has something to
stagger across at every motion preset.
