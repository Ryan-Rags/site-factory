---
title: Welding and repair
summary: A second invented service, so the services route renders more than one section.
highlights:
  - The second of two invented services
  - Proves the alternating section band on the services route
  - Belongs to a test fixture, not a client
---

Also invented. See `fabrication.md` — the two entries exist for the same
reason, and neither describes a capability anybody has.

The heading on this one is deliberately longer than the first, so the service
route's `h2` is measured by `check:textfit` against something that is not a
single short word.
