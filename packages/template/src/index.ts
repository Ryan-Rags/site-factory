/**
 * @site-factory/template — the config-driven Astro/Tailwind site template.
 *
 * One config object per business drives content, theme, and sections; the same
 * template renders both the outreach mockup and the delivered site, which
 * deploys to Cloudflare Pages.
 *
 * Skeleton only — no implementation yet.
 */

export const PACKAGE_NAME = "@site-factory/template";
